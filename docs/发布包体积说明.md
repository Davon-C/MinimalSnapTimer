# 发布包体积说明

## 当前发布策略
当前只保留完整版：

- `MinimalSnapTimer_v0.1.0-beta_win-x64.exe`

## 为什么 exe 比较大
因为它是：

- `.NET 8`
- `WPF`
- `win-x64`
- `self-contained`
- `single-file`

这意味着 .NET Desktop Runtime 和 WPF 依赖都被打包进同一个 exe。

## 为什么仍推荐完整版
- 普通用户无需安装运行时
- 直接双击即可运行
- 发布、分享、验收路径更简单
- 启动环境更稳定
